extends layout.pug

block content 
  article.grid-container.portfolio-intro
    div

      h1 My Portfolio
 
      p.lead.text-light
        | Here's some of the stuff I've made during the Treehouse Full Stack JavaScript TechDegree 🌳
  article.grid-container.portfolio-index
    
    .grid-x.grid-margin-x.small-up-2.medium-up-2.large-up-3
  
      each project in projects
        .cell
          a(href=`/projects/${project.id}`)
            img.thumbnail(src=`/images/Project${project.id}Homepage.png`)
            h5= project.project_name


    
      
